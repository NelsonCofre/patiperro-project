# Producción: sin DDL automático destructivo (ajustar si añades migraciones Flyway/Liquibase).
spring.jpa.hibernate.ddl-auto=validate

# DDL manual si validate: en tabla billetera agregar saldo_verificacion DECIMAL(14,2) NOT NULL DEFAULT 0;
# y crear tabla billetera_reserva_tracking (ver entidades JPA Billetera / BilleteraReservaTracking).
